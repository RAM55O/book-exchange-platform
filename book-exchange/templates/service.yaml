apiVersion: v1
kind: Service
metadata:
  name: {{ include "book-exchange.fullname" . }}
  labels:
    {{- include "book-exchange.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "book-exchange.selectorLabels" . | nindent 4 }}
